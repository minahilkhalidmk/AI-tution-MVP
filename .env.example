PORT=5000
NODE_ENV=production
JWT_SECRET=super_secret_jwt_key_ai_tuition_2026
COOKIE_SECRET=super_secret_cookie_key_ai_tuition_2026
CORS_ORIGIN=*
SERVER_URL=/

# MySQL Database Configuration (Optional — platform falls back to in-memory store if DB is unreachable)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ai_tuition_db
